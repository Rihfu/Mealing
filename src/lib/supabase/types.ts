/**
 * Type de la base de données Supabase, généré depuis le schéma.
 *
 * Source : `database.types.ts` (régénéré via la CLI/connecteur Supabase après
 * chaque migration). Ce ré-export sert de point d'import stable pour les clients
 * et les fonctions backend.
 */
export type { Database } from './database.types';
